/**
 * Compatibilidade: Re-exporta a base de dados do Encontro 1 da pasta /data/
 */
if (typeof ENCONTRO_1_DATA === 'undefined') {
  // Se ainda não foi carregado via /data/, busca da referência global
}
